{{- /* Sitemap: skip hidden-tagged pages (they are only reachable via their tag page) */ -}}
{{- $hidden := site.Params.hiddenTags | default slice }}
{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{- range .Site.AllPages -}}
  {{- $skip := false }}
  {{- if or .Params.robotsNoIndex (eq .Layout `archives`) (eq .Layout `search`) }}{{ $skip = true }}{{ end }}
  {{- if and (not $skip) $hidden (intersect (.Params.tags | default slice) $hidden) }}{{ $skip = true }}{{ end }}
  {{- if not $skip }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" }}</lastmod>{{ end }}{{ with .Sitemap.ChangeFreq }}
    <changefreq>{{ . }}</changefreq>{{ end }}{{ if ge .Sitemap.Priority 0.0 }}
    <priority>{{ .Sitemap.Priority }}</priority>{{ end }}
  </url>
  {{- end }}
  {{- end }}
</urlset>